// Multiply
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Random (greater than 0)
const random = Math.floor(Math.random() * 100) + 1;

// Mod (remainder should be 4)
const num3 = 20;
const num4 = 8;
const mod = num3 % num4;

// Max (should return 20 as the highest)
const max = Math.max(1, 3, 5, 10, 20);
